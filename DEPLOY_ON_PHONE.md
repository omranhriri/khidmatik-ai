# نشر خدمتك AI من الهاتف عبر Render

هذه النسخة أصبحت مهيأة لـ Render:
- Node/Express
- الاستماع على 0.0.0.0
- SQLite على `/var/data/khidmatik.db`
- متغيرات سرية للإدارة

## المطلوب منك
1. إنشاء حساب Render.
2. إنشاء مستودع GitHub ورفع محتويات هذا المجلد.
3. في Render اختر New → Web Service واربط المستودع.
4. استخدم:
   Build: `npm install`
   Start: `npm start`
5. أضف `ADMIN_USER` و`ADMIN_PASSWORD`.
6. اجعل `SESSION_SECRET` قيمة سرية طويلة، أو استخدم توليد Render.
7. يجب أن يكون التخزين الدائم مفعلاً لأن SQLite تحتاج إلى ملف محفوظ عبر عمليات إعادة التشغيل والنشر.

Render توضح أن نظام الملفات الافتراضي للخدمة مؤقت، وأن القرص الدائم يحفظ التغييرات؛ كما أن Web Service يحصل على نطاق `onrender.com` ويمكن إضافة نطاق مخصص لاحقاً.
